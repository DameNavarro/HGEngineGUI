<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="HGEngineGUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HGEngineGUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="HG Engine Tool">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <NavigationView x:Name="RootNavView" IsBackEnabled="False" IsSettingsVisible="False" PaneDisplayMode="Top" SelectionChanged="OnNavigationSelectionChanged">
        <NavigationView.MenuItems>
            <NavigationViewItem x:Name="StartItem" Content="Project" Tag="Start" Icon="Folder" IsSelected="True"/>
            <NavigationViewItem x:Name="PokemonItem" Content="Pokémon" Tag="Species" Icon="Contact"/>
            <NavigationViewItem x:Name="TrainersItem" Content="Trainers" Tag="Trainers" Icon="People"/>
            <NavigationViewItem x:Name="ItemsItem" Content="Items" Tag="Items" Icon="Shop"/>
            <NavigationViewItem x:Name="ConfigItem" Content="Config" Tag="Config" Icon="Setting"/>
            <NavigationViewItem x:Name="EncountersItem" Content="Encounters" Tag="Encounters" Icon="Map"/>
            <!-- Learnsets and Evolutions will be sub-pages under Pokémon, so we remove their top-level nav items. -->
        </NavigationView.MenuItems>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Frame x:Name="ContentFrame"/>
            <Grid Grid.Row="1" Background="#1E1E1E" Padding="8" ColumnDefinitions="Auto,*,Auto">
                <TextBlock x:Name="StatusProject" Foreground="LightGray"/>
                <TextBlock x:Name="StatusMessage" Grid.Column="1" Foreground="LightGray"/>
                <TextBlock x:Name="StatusTiming" Grid.Column="2" Foreground="LightGray"/>
            </Grid>
        </Grid>
    </NavigationView>
</Window>
